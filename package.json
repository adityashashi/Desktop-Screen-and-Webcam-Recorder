{
  "name": "capital-compute-technologies",
  "version": "1.0.0",
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "npm run start",
    "build": "npm run build:main && npm run build:renderer",
    "build:main": "tsc -p tsconfig.main.json",
    "build:renderer": "vite build",
    "start": "npm run build && electron .",
    "typecheck": "tsc -p tsconfig.main.json --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Screen and webcam recorder desktop app",
  "dependencies": {
    "electron": "^40.6.0",
    "ffmpeg-static": "^5.3.0",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
